#include<stdio.h>
#include<string.h>
int main(){
    int i,a[26],max;
    char c[10];
    for(i=0;i<26;i++)
    scanf("%d",&a[i] );
    scanf("%s",&c);
    max=a[0];
    for(i=0;i<strlen(c);i++){
        if(a[i]>max)
        max=a[i];
    }
    int e=max*strlen(c);
    printf("%d",e);


}
