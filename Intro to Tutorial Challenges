#include<stdio.h>
int main(){
    int n,i,v;
    scanf("%d",&v);
    scanf("%d",&n);
    int a[n]; 
    for(i=0;i<n;i++)
    scanf("%d",&a[i] );
    for(i=0;i<n;i++){
        if(a[i]==v)
        break;
    }
    printf("%d",i);
}
