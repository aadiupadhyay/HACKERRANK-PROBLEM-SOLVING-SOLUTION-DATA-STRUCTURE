#include<stdio.h>
int main(){
    int n,i,k;
    scanf("%d%d",&n ,&k);
    int a[n];
    for(i=0;i<n;i++){
        scanf("%d",&a[i] );
    }
    int max=a[0];
    for(i=1;i<n;i++){
        if(a[i]>max)
        max=a[i];
    }
    if(k<max)
    printf("%d",max-k);
    else
    printf("0");

}
