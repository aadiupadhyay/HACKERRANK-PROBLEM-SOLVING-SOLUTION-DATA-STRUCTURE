#include<stdio.h>
void staircase(int n)
{
    int i,j,k;
    for(i=1;i<=n;i++)
    {
        for(j=n-i;j>0;j--)
        {
            printf(" ");
        }
        for(k=0;k<i;k++)
        printf("#");
        printf("\n");
    }
}
int main()
{
    int n;
    scanf("%d",&n);
    staircase(n);
}
